"""Agent runtime: research-graph nodes, MCP client, and reliability wrappers.

`reliability` (cost cap, circuit breaker, retry, fallback, max-iter) is
the only sub-module populated in W1 — other nodes land in W2+.
"""
